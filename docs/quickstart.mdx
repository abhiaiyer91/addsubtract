---
title: Quick Start
description: "Get up and running with wit in 5 minutes"
---

# Quick Start

This guide will help you get started with wit in just a few minutes.

## Prerequisites

- Node.js 22.13.0 or higher
- npm or yarn

## Installation

```bash
# Clone and build wit
git clone https://github.com/nicholasgriffintn/wit.git
cd wit
npm install
npm run build
npm link   # Makes 'wit' available globally
```

## Your First Repository

### Initialize a Repository

```bash
mkdir my-project
cd my-project
wit init
```

You'll see:
```
Initialized empty wit repository in /path/to/my-project/.wit
```

### Add and Commit Files

```bash
# Create a file
echo "# My Project" > README.md

# Stage the file
wit add README.md

# Commit with a message
wit commit -m "Initial commit"
```

### Check Status

```bash
wit status
```

### View History

```bash
wit log
```

## Essential Commands

### Stage and Commit in One Step

```bash
# Stage all tracked files and commit
wit commit -a -m "Update everything"
```

### Quick WIP Save

```bash
# Save work-in-progress with auto-generated message
wit wip -a
```

### Switch Branches

```bash
# Create and switch to a new branch
wit switch -c feature-branch

# Switch back to main
wit switch main
```

<Note>
wit automatically saves your uncommitted changes when switching branches, so you never lose work!
</Note>

### Undo Mistakes

```bash
# Undo the last operation (commit, branch creation, etc.)
wit undo

# Undo the last 3 operations
wit undo --steps 3
```

## Visual Interfaces

### Web UI

```bash
wit web
```

Opens a modern dashboard at `http://localhost:3847` with:
- Visual commit graph
- Side-by-side diffs
- One-click staging
- Search across commits and files

### Terminal UI

```bash
wit ui
```

Interactive terminal interface with keyboard navigation.

### ASCII Graph

```bash
wit graph
```

```
● a1b2c3d4 (main) Latest commit - Alice, today
● e5f6g7h8 Add feature - Bob, yesterday
● i9j0k1l2 Initial commit - Alice, last week
```

## AI Features (Optional)

Enable AI-powered features for commit message generation, code review, and more.

### Setup

```bash
# Set your OpenAI or Anthropic API key
export OPENAI_API_KEY=sk-your-key-here
# OR
export ANTHROPIC_API_KEY=sk-ant-your-key-here

# Verify setup
wit ai status
```

### Use AI

```bash
# Natural language commands
wit ai "what files have I changed?"

# Generate commit message from staged changes
wit ai commit

# Stage all, generate message, and commit
wit ai commit -a -x

# Get code review
wit ai review
```

## Next Steps

<CardGroup cols={2}>
  <Card title="Commands Reference" icon="terminal" href="/commands/overview">
    Learn all available commands
  </Card>
  <Card title="Web UI Guide" icon="window" href="/visual/web-ui">
    Master the visual interface
  </Card>
  <Card title="AI Features" icon="robot" href="/features/ai-powered">
    Generate commit messages with AI
  </Card>
  <Card title="Configuration" icon="gear" href="/configuration/overview">
    Customize wit behavior
  </Card>
</CardGroup>
